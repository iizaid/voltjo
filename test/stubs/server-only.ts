// Test stub for the `server-only` package. In production this import throws if a
// module is ever pulled into a client bundle; under vitest (node env) it is a
// harmless no-op so server modules can be unit-tested directly.
export {};
